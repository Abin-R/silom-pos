[[source]]
url = "https://pypi.org/simple"
verify_ssl = true
name = "pypi"

[packages]
djangorestframework = ">=3.15"
django-cors-headers = ">=4.4"
python-dotenv = ">=1.0"
psycopg = {extras = ["binary"], version = ">=3.2"}
httpx = ">=0.27"
pillow = ">=10.0"
django = "<5.0,>=4.2"
sentry-sdk = {extras = ["django"], version = ">=2.0"}
# Association-rule mining for the cashier upsell strip.  Pure Python, no
# transitive dependencies, MIT.  Imported ONLY by the mine_suggestions
# management command — never by the web tier — so a box that missed this
# install degrades one cron job instead of failing deploy.sh's import gate.
efficient-apriori = ">=2.0,<3.0"

[dev-packages]

[requires]
python_version = "3.8"
